# Pingvin Share — environment variable reference
# Copy relevant lines to a .env file alongside docker-compose.yaml

# Timezone
TZ=America/New_York

# Service user/group IDs
SERVICE_USER=1000
SERVICE_GROUP=1000

# Hostname (used for the container hostname)
BASE_HOST_NAME=${HOSTNAME}
